tasks:
  - init: |
      pip install poetry
      poetry config virtualenvs.create false
      poetry install
vscode:
  extensions:
    - ms-python.python
    - matangover.mypy
ports:
  - port: 8000
    visibility: public
    onOpen: ignore